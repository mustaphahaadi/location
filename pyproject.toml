[project]
name = "location"
version = "0.1.0"
description = "HTTPS location consent links via ngrok"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.3",
    "python-dotenv>=1.2.2",
    "uvicorn>=0.48.0",
]
